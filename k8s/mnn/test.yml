---
- hosts: localhost
  vars:
    init: flannel
  tasks:
  - name: Install Flannel
    msg: "Flannel"
    when: init == "flannel"

  - name: Install Calico
    msg: "Calico"
    when: init == "calico"
